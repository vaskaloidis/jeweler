if ($("#success_message").is(':visible')) {
    $("#success_message").html('');
    $("#success_message").hide('slide');
}

if ($("#error_message").is(':visible')) {
    $("#error_message").html('');
    $("#error_message").hide('slide');
}

$("#invoice_panel_<%= @invoice_id.to_s %>_container").html('<%= j( render 'invoices/invoice_panel', invoice: @invoice, sprint: @sprint_current ) %></tr>');
// Invoice.where(project: @project, sprint: count).first, sprint: count
