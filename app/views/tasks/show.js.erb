$('#modal_target').html('<%= j render @task %>');
<%= render 'renderers/render_modal', title: 'View Task: ' + @task.task_id,
                                     display_close: true,
                                     small_modal: true %>