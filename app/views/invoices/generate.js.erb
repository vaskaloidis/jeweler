$("#modal_target").html("<%= j render 'invoices/select_customer', invoice: @invoice %>");

<% if @estimate %>
  <%= render 'renderers/render_modal', title: 'Generate Estimate' %>
<% else %>
  <%= render 'renderers/render_modal', title: 'Generate Invoice' %>
<% end %>