$("#modal_target").html("<%= j render 'invoices/select_customer', invoice: @invoice %>");

<% if @estimate %>
  <%= render 'renderers/render_modal', title: 'Estimate' %>
<% else %>
  <%= render 'renderers/render_modal', title: 'Invoice' %>
<% end %>