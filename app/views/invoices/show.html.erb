<div class="panel panel-default">
  <div class="panel-body">
    <div class="row">
      <div class="col-sm-12">
        <%#= render 'invoices/invoice_slider', invoice: project.current_sprint, sprint: project.current_sprint.sprint %>

        <div class="current_invoice_panel" id="invoice_panel_<%= @invoice.id.to_s %>_container">
          <%= render 'invoices/invoice_panel', invoice: @invoice, sprint: @invoice.sprint %>
        </div>

      </div>
    </div>
  </div>
</div>


<%= link_to 'Edit', edit_invoice_path(@invoice) %> |
<%= link_to 'Back', project_path(@invoice.project) %>
