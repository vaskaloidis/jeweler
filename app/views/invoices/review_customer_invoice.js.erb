<% if @error_msgs.empty? %>
$("#modal_target").html('<%= j render'sprints/review_invoice_customer', invoice: @sprint, estimate: @estimate, customer:@customer, invoice_note:@invoice_note, display_payments: @display_payments, payment_request_amount:@request_amount, customer_email:@customer_email, invitation:@invitation %>');
<% else %>
  <% @error_msgs.each do |error| %>
    toastr.error('<%= error %>');
  <% end %>
  $("#modal_target").html('<%= j render'sprints/invoice_select_customer', invoice: @sprint, estimate: @estimate %>');
<% end %>