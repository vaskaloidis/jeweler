<%= render 'invoices/re-render_invoice_panel', invoice: @invoice %>
<%= render 'invoices/re-render_invoice_panel', invoice: @old_invoice %>

<%= render 'notes/re-render_timeline', notes: @invoice.project.sprint_notes, project:@invoice.project %>
<%= render 'payments/re-render_payments_panel', project: @invoice.project, user: current_user %>

toastr.success('Sprint Changed To <%= @invoice.sprint.to_s %> From <%= @old_invoice.sprint.to_s %> ');

$('#sprint_gauge_container').animate({
    width: '0'
});

$('#sprint_gauge_container').animate({
    width: '<%= ApplicationHelper.sprint_percent(@invoice.project) %>%'
});


// Invoice.where(project: @project, sprint: count).first, sprint: count
