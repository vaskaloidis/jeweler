$('.sprint-displayed').removeClass('sprint-displayed');

$('.sprint-panel.active').removeClass('active').slideUp();
$('#sprint-panel-<%= @sprint.sprint.to_s %>').addClass('active');

$("#sprint-panel-<%= @sprint.sprint.to_s %>-container").html('<%= j render 'sprints/sprint', sprint: @sprint  %>');

$('#sprint-panel-<%= @sprint.sprint.to_s %>').slideDown();

$('li > #sprint_btn_<%= @sprint.sprint.to_s %>').addClass('sprint-displayed');

<%#= j render 'renderers/re-render_sprint_panel', sprint: @project.get_sprint(sprint) %>
<%#= j @sprint.render_panel %>
<%#= j render 'renderers/re-render_sprint_panel', sprint: @sprint %>

// TODO:Check if its empty to prevent useless re-rendering
// if( $('#sprint_panel_<%#= @sprint.sprint.to_s %>_container').html() == '' ) {
//     $("#sprint_panel_<%= @sprint.sprint.to_s %>_container").html('<%#= j @sprint  %>');
// }
