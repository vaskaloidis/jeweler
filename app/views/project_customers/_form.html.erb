<%= form_with(model: project_customer,  url: new_project_customer_path, local: false) do |form| %>
  <% if project_customer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project_customer.errors.count, "error") %> prohibited this project_customer from being saved:</h2>

      <ul>
      <% project_customer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= select("user", "user_id", @project.non_customers {|u| [ u.id ] }, { include_blank: false }) %>
<!--
    <%#= form.collection_select(:user_id,
                               User.all,
                               :user_id,
                               :first_last_name_email,
                               {:prompt => "Select A User"}) %>
  -->
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
