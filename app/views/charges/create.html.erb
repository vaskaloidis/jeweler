<%= render 'charges/payment_details', payment: @payment, errors: @error_msgs %>

<% unless @error_msgs.empty? %>
  <script>
      <% @error_msgs.each do |error| %>
      toastr.error('<%= error %>');
      <% end %>
  </script>
<% end %>