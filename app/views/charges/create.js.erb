$('#modal_target').html('<%= j render 'charges/payment_details', payment: @payment, errors:@error_msgs %>');
<%= render 'renderers/render_modal', title: 'Payment Details', display_close: true %>

<% unless @error_msgs.empty? %>
  <% @error_msgs.each do |error| %>
    toastr.error('<%= error %>');
  <% end %>
<% end %>